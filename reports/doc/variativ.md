# Отчёт по вариативной части

## Выбор темы

Я выбрал тему: **Static Site Generator in 40 lines with Node.js**

## Стек технологий

- Node.js (fs, path)
- JavaScript
- Markdown

## Исследование

Изучил принцип работы генераторов статических сайтов. Понял, что:
- Markdown превращается в HTML через замену спецсимволов
- Файлы читаются и записываются через модуль fs

более подробно я расписал это в техническом руководстве (rukovod.md`).

## Модификация

я добавил подсчёт слов в статью с помощью:

```javascript
const wordCount = md.split(/\s+/).length;